import React from 'react'

export default React.createClass({
  render: () => {
    return (
      <div id="progress-bar" className=" progress progress-striped active full-width" >
        <div className="progress-bar progress-bar-info full-width"></div>
      </div>
    )
  }
})
